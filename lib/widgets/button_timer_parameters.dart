import 'package:flutter/material.dart';

class ButtonTimerParametrs {
  final Text text;
  final ButtonStyle activeColor;
  final ButtonStyle inactiveColor;

  ButtonTimerParametrs(this.text, this.activeColor, this.inactiveColor);
}
